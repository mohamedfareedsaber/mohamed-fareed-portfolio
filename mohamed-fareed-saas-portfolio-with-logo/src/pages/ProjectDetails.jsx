import { useParams, Link } from 'react-router-dom'
import { projects } from '../data/siteData.js'
import { useApp } from '../context/AppContext.jsx'
import Icon from '../components/Icon.jsx'
export default function ProjectDetails(){ const {slug}=useParams(); const {isAr}=useApp(); const p=projects.find(x=>x.slug===slug) || projects[0]; const features=isAr?p.featuresAr:p.featuresEn; return <section className="section page"><Link className="back" to="/projects">← {isAr?'رجوع للمشاريع':'Back to projects'}</Link><div className="case-study"><span>{p.tag}</span><h1>{isAr?p.titleAr:p.titleEn}</h1><p>{isAr?p.descAr:p.descEn}</p><div className="chips big">{p.stack.map(s=><small key={s}>{s}</small>)}</div><div className="case-grid"><div><h3>{isAr?'المشكلة':'Problem'}</h3><p>{isAr?'وجود عمليات متكررة وتحتاج تنظيم، صلاحيات، تقارير، وربط بين الأنظمة بدون فوضى.':'Repetitive operations need structure, permissions, reporting, and system integrations without chaos.'}</p></div><div><h3>{isAr?'الحل':'Solution'}</h3><p>{isAr?'بناء نظام واضح قابل للتوسع مع تجربة استخدام حديثة ومعمارية قوية وموديولات منظمة.':'Build a clear scalable system with modern UX, strong architecture, and organized modules.'}</p></div><div><h3>{isAr?'المميزات':'Features'}</h3><ul>{features.map(f=><li key={f}><Icon name="check" size={16}/>{f}</li>)}</ul></div><div><h3>{isAr?'النتيجة':'Outcome'}</h3><p>{isAr?'نظام أسهل في الإدارة، أسرع في التشغيل، وقابل للتطوير بدون إعادة بناء من الصفر.':'A system easier to manage, faster to operate, and ready to scale without rebuilding from scratch.'}</p></div></div></div></section> }
